version '1.0-SNAPSHOT'

subprojects {
    apply plugin: "java"
    group 'edu.thecop.tools'

    tasks.withType(JavaCompile) {
        sourceCompatibility = 1.8
        targetCompatibility = 1.8
    }

    repositories {
        mavenCentral()
    }
}
